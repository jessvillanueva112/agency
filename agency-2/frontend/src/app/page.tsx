// If you are still seeing old styles or fonts, try running the frontend on a different port (e.g., 3001) to avoid browser cache issues.
'use client';
import AIAssistantDashboard from '../components/AIAssistantDashboard';

export default function Home() {
  return (
    <main>
      <AIAssistantDashboard />
    </main>
  );
}